# Aurora Monitoring System
A high-performance monitoring system for tracking system metrics and performance.

## Getting Started
1. Run `go mod init aurora-monitoring-system` to initialize the Go module.
2. Run `go get` to install dependencies.
3. Run `go run src/main.go` to start the server.
